[project]
name = "kokopop"
version = "0.1.0"
description = "Standalone Kokoro GGML Runtime"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "huggingface-hub>=1.13.0",
    "kokoro>=0.9.4",
    "pypinyin>=0.55.0",
    "torch>=2.11.0",
]
