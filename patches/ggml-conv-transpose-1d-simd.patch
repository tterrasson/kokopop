diff --git a/src/ggml-metal/ggml-metal-ops.cpp b/src/ggml-metal/ggml-metal-ops.cpp
index 1c3bb936..af9104cd 100644
--- a/src/ggml-metal/ggml-metal-ops.cpp
+++ b/src/ggml-metal/ggml-metal-ops.cpp
@@ -4495,7 +4495,8 @@ int ggml_metal_op_conv_transpose_1d(ggml_metal_op_t ctx, int idx) {
     ggml_metal_encoder_set_buffer  (enc, ggml_metal_get_buffer_id(op->src[1]), 2);
     ggml_metal_encoder_set_buffer  (enc, ggml_metal_get_buffer_id(op),         3);
 
-    ggml_metal_encoder_dispatch_threadgroups(enc, OL, OC, 1, 1, 1, 1);
+    // 32 threads per group: each thread handles IC/32 channels, simd_sum reduces.
+    ggml_metal_encoder_dispatch_threadgroups(enc, OL, OC, 1, 32, 1, 1);
 
     return 1;
 }
diff --git a/src/ggml-metal/kernels/conv.metal b/src/ggml-metal/kernels/conv.metal
index 5685b5cd..159930b2 100644
--- a/src/ggml-metal/kernels/conv.metal
+++ b/src/ggml-metal/kernels/conv.metal
@@ -289,7 +289,8 @@ kernel void kernel_conv_transpose_1d(
         device const float * src1,
         device        char * dst,
         uint3   tgpig[[threadgroup_position_in_grid]],
-        uint3   tgpg[[threadgroups_per_grid]]) {
+        uint3   tgpg[[threadgroups_per_grid]],
+        ushort  tiitg[[thread_index_in_threadgroup]]) {
 
     // For output position j on the time axis, only input positions
     //   i such that i*s0 <= j < i*s0 + K
@@ -311,7 +312,8 @@ kernel void kernel_conv_transpose_1d(
 
     float v = 0.0f;
     if (i_min <= i_max) {
-        for (int64_t c = 0; c < args.IC; c++) {
+        // 32 threads split the IC loop; simd_sum reduces across the SIMD group.
+        for (int64_t c = tiitg; c < args.IC; c += 32) {
             const int32_t kernel_offset = c * tgpg[1] * K + K * tgpig[1];
             const int32_t input_offset  = c * IL;
 
@@ -321,9 +323,12 @@ kernel void kernel_conv_transpose_1d(
         }
     }
 
-    device float * dst_ptr = (device float *) (dst + tgpig[0] * args.nb0 + tgpig[1] * args.nb1);
+    v = simd_sum(v);
 
-    dst_ptr[0] = v;
+    if (tiitg == 0) {
+        device float * dst_ptr = (device float *) (dst + tgpig[0] * args.nb0 + tgpig[1] * args.nb1);
+        dst_ptr[0] = v;
+    }
 }
 
 template [[host_name("kernel_conv_transpose_1d_f32_f32")]]
@@ -333,7 +338,8 @@ kernel void kernel_conv_transpose_1d<float>(
     device const float * src1,
     device        char * dst,
     uint3   tgpig[[threadgroup_position_in_grid]],
-    uint3    tgpg[[threadgroups_per_grid]]);
+    uint3    tgpg[[threadgroups_per_grid]],
+    ushort  tiitg[[thread_index_in_threadgroup]]);
 
 template [[host_name("kernel_conv_transpose_1d_f16_f32")]]
 kernel void kernel_conv_transpose_1d<half>(
@@ -342,7 +348,8 @@ kernel void kernel_conv_transpose_1d<half>(
     device const float * src1,
     device        char * dst,
     uint3   tgpig[[threadgroup_position_in_grid]],
-    uint3    tgpg[[threadgroups_per_grid]]);
+    uint3    tgpg[[threadgroups_per_grid]],
+    ushort  tiitg[[thread_index_in_threadgroup]]);
 
 
 typedef void (conv_transpose_2d_t)(
